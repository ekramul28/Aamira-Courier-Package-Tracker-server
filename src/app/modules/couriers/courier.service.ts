export const updateCourierLocation = async (userId, locationData) => {
  // Implement upsert logic for courier live location
  // Example: LiveLocation.findOneAndUpdate({ user: userId }, locationData, { upsert: true, new: true })
};
